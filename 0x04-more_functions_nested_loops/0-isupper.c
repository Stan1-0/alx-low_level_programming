#include "main.h"
/**
 * _isupper - checks for uppercase character
 * @c: character to be checked
 * Return: 1(uppercase) or 0(otherwise)
 */

int _isupper(int c)
{
if ((c >= 'A') && (C <= 'Z'))
return (1);

return (0);
}
